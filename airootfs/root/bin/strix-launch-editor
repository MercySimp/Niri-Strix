#!/bin/bash

strix-cmd-present "$EDITOR" || EDITOR=nvim

case "$EDITOR" in
nvim | vim | nano | micro | hx | helix)
  xdg-terminal-exec  -e "$EDITOR" "$1" "${@:2}"  "$@"
  ;;
*)
  exec setsid "$EDITOR" "$@"
  ;;
esac
